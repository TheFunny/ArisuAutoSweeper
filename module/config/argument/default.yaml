# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

Cafe:
  Scheduler:
    Enable: true
Mail:
  Scheduler:
    Enable: true
Circle:
  Scheduler:
    Enable: true
Bounty:
  Scheduler:
    Enable: true
TacticalChallenge:
  Scheduler:
    Enable: true