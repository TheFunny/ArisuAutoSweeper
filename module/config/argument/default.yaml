# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

Alas:
  Emulator:
    ScreenshotMethod: scrcpy
Cafe:
  Scheduler:
    Enable: true
Mail:
  Scheduler:
    Enable: true
Circle:
  Scheduler:
    Enable: true
Bounty:
  Scheduler:
    Enable: true
Scrimmage:
  Scheduler:
    Enable: true
TacticalChallenge:
  Scheduler:
    Enable: true